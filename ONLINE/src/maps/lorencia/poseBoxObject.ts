import { ModelObject } from '../../common/modelObject';

export class PoseBoxObject extends ModelObject {
  async init() {
    // LightEnabled = true;

    // await this.loadSpecificModel(`PoseBox01.glb`);

    this.Ready = true;
  }
}
